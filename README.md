# Any-Base-Converter
Implemented a C progam hat converts numbers between any two bases up to base 10. Enter your number, specify which base you're converting from and which base you want as the output - converting to decimal first, then to your desired base. Runs in a continuous loop so you can convert multiple numbers in one session without restarting the program.
